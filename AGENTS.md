# RUVO Project Sync - Agent Communication Log

## ⚠️ INSTRUCTIONS FOR ALL AI AGENTS
Before making any code changes or writing a plan, you MUST read this file.
When you complete a task, you MUST append a short summary of your changes to the "Agent Work Log" at the bottom of this file. Do not overwrite previous logs.

## 🧱 Current Architecture State (LOCKED - DO NOT OVERWRITE)

**1. Core Stack & Environment:**
* **Framework:** React Native (0.81.5) + Expo (SDK 54).
* **Styling:** React Native `StyleSheet`. Custom fonts (`Poppins`) are already loaded globally. Do not use Tailwind or NativeWind.

**2. Navigation (`App.js` is the Source of Truth):**
* We are using `@react-navigation/stack` directly in `App.js` (ignore the `app/` folder for routing).
* **Auth Gating:** Navigation is strictly gated by `useUser()` from `src/context/UserContext`. 
* **Flow Logic:** If `user` is null -> Guest Flow. If `user` exists but `!userData?.onboardingCompleted` -> Onboarding Flow. Otherwise -> Main App Flow.

**3. Database & Authentication (`src/config/firebase.js`):**
* **Firebase (v12.8.0):** Modular SDK is fully initialized.
* **Auth:** Firebase Auth is configured with `getReactNativePersistence` using `@react-native-async-storage/async-storage`. Do not rewrite auth persistence.
* **Database:** Firestore is initialized and exported as `db`.
* **Storage:** Firebase Storage is initialized and exported as `storage`.

**4. State Management:**
* Global state is handled via React Context (`UserContext`, `ThemeContext`, `NotificationContext`). 
* When building UI components, ALWAYS check if the data can be destructured from `useUser()` before writing a direct Firestore fetch.

**5. Monetization (`src/services/revenueCat.js`):**
* **Provider:** `react-native-purchases` is fully initialized.
* **Entitlement Check:** The active premium entitlement string is EXACTLY `'Ruvo Pro'`.

## 🏗️ Active Roles
* **Agent 1 (Claude):** Lead Backend & Logic Engineer (Focus: RevenueCat, database schemas, API routes, data logic).
* **Agent 2 (Gemini):** Lead Product Architect & UI Designer (Focus: UI components, styling, user flow, component structure).

## 🎯 Current Global Objective
* Completed: Integrate RevenueCat paywall and Injury/Vacation Logic.
* Completed: Integrate `RuvoDashboard.tsx` Bento-Box UI.
* Next: [Waiting for new objective]

## 📝 Agent Work Log
* [2026-02-11] - Agent 1 (Claude): Implemented RevenueCat integration (Service, Native Paywall, Customer Center) and Injury/Vacation logic (aiCoach.js, UserContext.js). Verified entitlements and native UI components.
* [2026-02-11] - Agent 2 (Gemini): Created `src/components/RuvoDashboard.js` featuring a Bento-Box grid layout, Neon styling (#CCFF00), and integrated RevenueCat entitlement checks for the Pro banner. Ready for integration.
* [2026-02-11] - Agent 1 (Claude): Fixed infinite loading issue in `UserContext.js` by wrapping auth/RevenueCat initialization in a `try/catch/finally` block. App should now load even if RevenueCat fails.
* [2026-02-11] - Agent 2 (Gemini): UI is ready to be hooked up to the database. (Note for Claude: Dashboard component is implemented and ready for deeper data integration if needed).
* [2026-02-14] - Agent 2 (Gemini): Connected `RuvoDashboard.js` to live user data. Added current streak calculation and display card. Dashboard now shows: weekly goal progress, coins balance, AI coach shortcut, Pro upgrade banner (conditional), and running streak with fire icon.
* [2026-02-14] - Agent 2 (Gemini): Redesigned `PaywallScreen.js` with a custom high-conversion UI. Features dark mode, neon accents (#CCFF00), feature list with checkmarks, and dynamic pricing cards using RevenueCat data. Refined feature list: 2x Coins, AI Coaching, Analytics, Pro Plans. Replaces generic paywall.
* [2026-02-19] - Agent 2 (Gemini): Overhauled `AICoachScreen.js` into a Premium "Coach Dashboard". Replaced empty chat state with a Zero State containing Quick Action cards (Analyze, Plan, Recover). Implemented Rich Message rendering for Workout Cards (mocked UI). Added "Ask Coach" button to `PlanScreen.js` header for seamless integration. Fixed critical bugs (Navigation Reset, Paywall NaN, Startup Freeze).
* [2026-02-19] - Agent 1 (Claude): Connected NotificationContext to Firestore for persistent push notifications. Validated CommunityScreen onSnapshot listeners and confirmed Training Plan completion tracking inside UserContext.
* [2026-03-11] - Agent 1 (Claude): UX Audit Critical Bug Fixes across Auth & Onboarding flow. Fixed: (1) LoginScreen silent social login failures + biometric auto-login missing MFA handling, (2) SignUpScreen silent social login failures, (3) OnboardingScreen handleFinalSave missing try/catch + added saving state to prevent double-taps, (4) OnboardingSignUpScreen profile write failure after signup now has retry mechanism to prevent devastating onboarding loop + social login buttons now persist onboarding data to Firestore.
* [2026-03-14] - Agent 2 (Gemini): Audited and fixed `ForgotPasswordScreen.js` logic. (1) Implemented a 60-second cooldown timer to prevent spamming the password reset email. (2) Added a clear "Remember your password? Log in" escape route at the bottom of the form. (3) Verified and documented that modern Firebase silently succeeds for non-existent emails to prevent enumeration attacks, so the success alert appropriately handles both valid and invalid emails without exposing user privacy.
* [2026-03-14] - Agent 2 (Gemini): Audited and fixed `MfaVerificationScreen.js` logic. (1) Implemented a 30-second SMS resend cooldown. (2) Added a clear "Cancel & Return to Login" escape route. (3) Implemented graceful handling of Firebase session expiration (`auth/code-expired`), routing users safely back to Login. (4) Patched a critical React Navigation edge case where retrieving a stale `resolver` object from a backgrounded OS state caused hard crashes due to stripped functions; the screen now securely validates `resolver.resolveSignIn` on mount.
* [2026-03-14] - Agent 2 (Gemini): Fixed `OnboardingScreen.js` Step 5 Google Sign-In bug. The setup now correctly verifies `res?.success` strictly from the `loginWithGoogle` helper, preventing the user layout from routing away to the Dashboard prematurely upon cancellation (`SIGN_IN_CANCELLED` or `12501`) or general failures, and properly displaying inline alerts while preserving the onboarding state.
* [2026-03-14] - Agent 2 (Gemini): Fixed a "No ID token returned" bug in `UserContext.js` during Google Sign-In. The setup was upgraded to support `@react-native-google-signin/google-signin` v16+ by explicitly invoking `await GoogleSignin.getTokens()` to retrieve the `idToken` payload, which is no longer reliably bundled within the `signIn()` response on Android to save payload size.
* [2026-03-20] - Agent 1 (Claude): Fixed `FloatingNavBar.js` fake tab bar stacking bug. Replaced `navigation.navigate()` with `CommonActions.reset()` to prevent infinite screen accumulation in the Stack navigator. This fixes: (1) memory leak from ever-growing navigation stack, (2) broken Android back button cycling through tab history instead of exiting, (3) loss of scroll state when switching back to a tab.
* [2026-03-20] - Agent 2 (Gemini): Wired `ActiveRunScreen.js` to correctly route through `RateEffortScreen.js`. Fixed `SaveActivityScreen.js` critical image upload flaw by implementing `uploadBytes` to Firebase Storage before sending data to the Cloud Function.
* [2026-03-20] - Agent 2 (Gemini): Logged `PastRunDetailScreen` (rendering map and splits from history) as a post-launch v1.1 feature. Runs remain a static list for launch to prioritize core flows.
* [2026-03-20] - Agent 2 (Gemini): Logged minor double-tap risk in `WorkoutDetailScreen.js` ("Start Workout" button stacking ActiveRunScreen instances) as a v1.1 polish item.
* [2026-03-20] - Agent 2 (Gemini): Logged full Privacy System (`checkPrivacyPermission`) as a post-launch v1.1 feature. Removed broken privacy checks from `UserProfileScreen` to default all profiles to public for launch.
* [2026-03-20] - Agent 1 (Claude): Fixed `followUser` crash in `UserContext.js` — removed broken `checkPrivacyPermission` call (v1.1 feature). Fixed `FindFriendsScreen.js` to show "Requested" state for pending friend requests and prevent duplicate follow attempts.
* [2026-03-20] - Agent 1 (Claude): Audited `ClubDetailScreen.js` — verified all 6 club functions (`deleteClub`, `updateClub`, `toggleClubPostLike`, `addClubComment`, `acceptClubRequest`, `declineClubRequest`) exist in `UserContext.js`. Fixed `addClubComment` signature mismatch (was passing object, function expects string). Fixed `CreateClubScreen.js` `handleCreate` to use async/await with try/catch error handling.
* [2026-03-20] - Agent 1 (Claude): Audited `ChatScreen.js`. Identified 4 minor post-launch (v1.1) polish items: (1) Add pagination (limit) to message fetch to prevent performance drops on large histories, (2) Add `isSending` lock on Send button to prevent rapid-tap duplicate sends, (3) Fix 100ms `setTimeout` auto-scroll stutter, (4) Handle offline write rejection gracefully to prevent drafted text loss.
* [2026-03-20] - Agent 1 (Claude): Audited and fixed `SearchScreen.js`. (1) Added 300ms debounce to `handleSearch` — was firing a Firestore query on every single keystroke, burning read quota. (2) Added "Requested" state to follow button, matching `FindFriendsScreen` and `UserListScreen` pattern to prevent duplicate friend requests.
* [2026-03-20] - Agent 1 (Claude): Audited `AICoachScreen.js`. Identified 2 minor post-launch (v1.1) items: (1) No rate-limiting on "Send" or Quick Action buttons, leading to potential Cloud Function spam and quota waste. (2) Low-risk privacy issue: User's weight is sent to Gemini as part of the system prompt context. Neither is considered blocking for launch.
* [2026-03-20] - Agent 1 (Claude): Audited `AnalyticsScreen.js` and `PlanScreen.js`. Identified CRITICAL v1.1 Tech Debt: `runHistory` is currently stored as an unbound array field directly on the `users` Firestore document. At ~500+ runs, active users will hit the Firestore 1MB document max size limit, permanently bricking their accounts. Must migrate to a `users/{uid}/runs/` subcollection in v1.1. Plan/Analytics screens otherwise have perfect empty state and offline handling.
* [2026-03-20] - Agent 1 (Claude): Audited all 9 Profile & Settings screens (ProfileScreen, EditProfileScreen, GearScreen, AchievementsScreen, ReferralScreen, SettingsScreen, SettingsDetailScreen, PrivacyControlsScreen, 2FASetupScreen). No ghost functions found. Findings: (1) GearScreen `addGear`/`updateGear`/`deleteGear` calls lack try/catch — silent failures. (2) SettingsDetailScreen "Regenerate Plan" wipes `runHistory: []` — catastrophically destructive, must be gated or removed. (3) 2FASetupScreen `FirebaseRecaptchaVerifierModal` is commented out, so `recaptchaVerifier.current` is null — enrollment will crash on Send Code. (4) ProfileScreen country picker `updateUserProfile` call has no try/catch. (5) SettingsDetailScreen notification toggles fire `updateUserProfile` without error handling. All screens have proper empty state handling and no stale Firestore listeners.
* [2026-03-25] - Agent 2 (Gemini): Executed all 5 Profile & Settings fixes identified by Claude. (1) Added `try/catch` and confirmation logic to `GearScreen.js`. (2) Gated "Regenerate Plan" in `SettingsDetailScreen.js` with a destructive confirmation alert. (3) Restored `FirebaseRecaptchaVerifierModal` in `2FASetupScreen.js` to fix SMS crash. (4) Added `try/catch` revert logic to country picker in `ProfileScreen.js`. (5) Added `try/catch` to notification toggles in `SettingsDetailScreen.js`. Re-ignored temp & build files in `.gitignore`.
* [2026-03-25] - Agent 2 (Gemini): Completed Health Data UI Integration. (1) Dashboard shows live steps, resting HR, and readiness. (2) ActiveRunScreen displays dynamic Heart Rate and Zones 1-5. (3) SaveActivityScreen and AnalyticsScreen now render HR zone breakdown charts. (4) Injected biometric recovery data into Gemini AI Coach prompt for tailored advice.
* [2026-03-25] - Agent 1 (Claude): Implemented Apple HealthKit and Google Health Connect native integration. Configured native Android permissions and `app.json` Expo plugins. Created `healthService.js` abstraction layer for cross-platform API calls. Refactored `UserContext.js` to fetch real Steps and Resting HR, and wired `SaveActivityScreen.js` to automagically sync published runs back to Apple Health / Health Connect.
* [2026-03-25] - Agent 2 (Gemini): Integrated Whoop OAuth 2.0 flows via `expo-auth-session` and `expo-web-browser` mapping to `SettingsScreen.js` connected devices UI. Implemented `syncWhoopData` Cloud Function to fetch Recovery, Strain, and Sleep metrics automatically upon token generation, securely persisting access tokens to `expo-secure-store`. Verified AI Prompt Context recognizes the biometric footprint.
* [2026-03-25] - Agent 2 (Gemini): Integrated Oura Ring API v2 via `expo-auth-session` OAuth flow mapping to `SettingsScreen.js` connected devices UI alongside Whoop. Built `syncOuraData` Cloud Function to fetch Daily Readiness, Sleep, Heart Rate, and Activity. Injected explicit `ouraData` into Gemini AI Coach prompt for tailored biometric advice.
* [2026-03-25] - Agent 2 (Gemini): Built `ConnectedDevicesScreen.js` grouping Apple Health, Google Health Connect, Whoop, and Oura Ring. Updated `SettingsScreen.js` to add a new Connected Devices row linking to the screen. Added Daily Steps & Resting HR widgets to `HomeScreen.js`. Added a large visual HR Zone segment widget to `ActiveRunScreen.js` and a horizontal % time in zone chart to `AnalyticsScreen.js`.
* [2026-03-26] - Agent 2 (Gemini): Built complete Rewards Ecosystem. Updated `redeemReward` cloud function to generate and fulfill digital codes, and mock physical shipment email payloads. Built `RewardsStoreScreen.js` for browsing and transacting, and `MyRedemptionsScreen.js` for tracking shipping status and viewing digital codes via real-time Firestore listeners.
* [2026-03-30] - Agent 2 (Gemini): Completed Premium Feel initiative. (1) Injected `lightTap`/`successFeedback`/`errorFeedback` + `activeOpacity={0.7}` into `RewardsStoreScreen.js` and `MyRedemptionsScreen.js`. (2) Created `useStaggerAnimation.js` hook and applied `Animated.FlatList` staggered entrance to `RewardsScreen.js`, `LeaderboardScreen.js`, `GearScreen.js`. (3) Installed `react-native-skeleton-placeholder`, created `SkeletonCard.js` (4 variants), replaced `ActivityIndicator` with skeleton loaders in `HomeScreen.js` and `LeaderboardScreen.js`.
* [2026-03-31] - Agent 1 (Claude): Premium Feel Phase 2. (1) FloatingNavBar scale-press animation with navigation lock. (2) Created `AnimatedCounter.js` (reusable count-up component), `AchievementOverlay.js` (full-screen badge unlock with particles + scale spring + glow), `StreakMilestone.js` (celebration at 7/14/30/60/100/200/365-day streaks). (3) Added `post` and `chat` skeleton variants to `SkeletonCard.js`. (4) Pull-to-refresh (`RefreshControl`) on HomeScreen, CommunityScreen, ProfileScreen. (5) Animated XP bar fill on HomeScreen. (6) Wired animated reward Counter into SaveActivityScreen badge modal. (7) Wired StreakMilestone celebration into HomeScreen.
* [2026-04-02] - Agent 1 (Claude): Premium Feel Phase 2 Continuation. (1) Refactored `SaveActivityScreen.js` inline `Counter` → reusable `AnimatedCounter.js` component. (2) Wired `AchievementOverlay.js` full-screen celebration into SaveActivityScreen badge flow (shows before badge modal on new badge unlock). (3) Added `SkeletonCard` `post` variant to `CommunityScreen` via `FeedTab` (passes `isInitialLoad` prop, shows 3 post skeletons during Firestore load). (4) Added `SkeletonCard` `chat` variant + `RefreshControl` to `ChatScreen`. (5) Fixed `RewardsScreen` and `GearScreen` `activeOpacity={0.9}` → `{0.7}`. (6) Added `RefreshControl` + skeleton loaders (`chart` variant) to `AnalyticsScreen`. (7) Added `lightTap` + `activeOpacity={0.7}` to `SearchScreen` (back button, clear button) and `AchievementsScreen` (back button, modal close). (8) Added `lightTap` + `activeOpacity={0.7}` to `PaywallScreen` (close, CTA Subscribe, Restore, Terms, Privacy links). (9) Added `RefreshControl` + skeleton loaders + `lightTap` + tab haptic to `RewardsScreen`. (10) Set up Playwright E2E test suite: installed `@playwright/test` + `playwright` + `serve`, created `playwright.config.js` with static file server, wrote 34 smoke/screen tests across all premium feel screens (`saveActivityScreen`, `communityScreen`, `chatScreen`, `analyticsScreen`, `paywallScreen`, `searchScreen`, `achievementsScreen`, `rewardsScreen`). Fixed GearScreen syntax error and missing native dependencies for web export. All 34 tests pass.
* [2026-04-02] - Agent 1 (Claude): Rewrote Android section of `healthService.js` to use `react-native-health-connect` v3 API. (1) `syncRunToHealth`: Fixed `DistanceRecord` from v2's `distance: { inMeters }` → v3's `distance: { value, unit: 'meters' }`, fixed `ActiveCaloriesBurnedRecord` from v2's `energy: { inKilocalories }` → v3's `energy: { value, unit: 'kilocalories' }`, added `ExerciseSessionRecord` with `exerciseType: ExerciseType.RUNNING` (56) for semantic workout grouping. (2) `fetchTodayStats`: Replaced `readRecords` with `aggregateRecord` for Steps (reads `COUNT_TOTAL`), HeartRate (reads `BPM_AVG`), and ActiveCaloriesBurned (reads `ACTIVE_CALORIES_TOTAL.inKilocalories`) — all 3 run in parallel via `Promise.all`. (3) Added `ExerciseSession` write permission to `androidPermissions`. (4) Imported `aggregateRecord` and `ExerciseType` from `react-native-health-connect`.
* [2026-04-02] - Agent 1 (Claude): Completed Whoop integration. `whoopService.js` (OAuth 2.0 via `expo-auth-session`, token exchange at `api.prod.whoop.com`, secure storage via `expo-secure-store`, Cloud Function sync) and `syncWhoopData` Cloud Function (fetches `/recovery`, `/cycle`, `/sleep`, `/user/measurement/heart_rate` in parallel, saves `whoopData` to `users/{uid}`) were already built. `aiService.js` already injects `whoopData` into the AI system prompt. `SettingsScreen.js` already has `handleWhoopConnect`. **Fixed:** `ConnectedDevicesScreen.js` was using hardcoded mock booleans instead of real service. Rewrote it to: (1) import and use real `whoopService`/`ouraService`, (2) read connection status from `userData?.whoopData`/`userData?.ouraData` (via Firestore `onSnapshot` listener), (3) wire Connect/Disconnect/Sync buttons to actual OAuth flows, (4) add loading spinners during auth/sync, (5) add live biometric chips (Recovery %, Strain, HRV, Sleep Score) shown when connected, (6) add upgrade banner for non-Pro users. Also fixed `connectWhoop`/`connectOura` in `UserContext.js` which called non-existent service methods — simplified to just `authenticate()` (sync is already handled inside that call).
* [2026-04-02] - Agent 1 (Claude): Built complete digital rewards redemption flow with QR-code email delivery. **Upgraded `redeemReward`** Cloud Function: generates cryptographically random codes (`RUVO-XXXX-XXXX-XXXX`), sets 30-day expiry, stores `discountCode` + `status: "active"` + `uid` + `expiresAt` in `users/{uid}/redemptions/{id}`. Added `sendRewardEmail()` helper that generates QR code (via `qrcode` package) as base64 PNG, embeds it in a branded HTML email, and sends via Resend API (`resend` SDK). **Added `verifyRewardCode`** public HTTP endpoint: reads code, looks up redemption across user subcollections, returns validity, user name, reward title, and redemption date. **Added `redeemRewardCode`** public HTTP endpoint: marks code as `used` after store staff taps "Approve Discount." Created `public/verify.html` — a dark-themed, mobile-first verification page for store staff (shows VALID/INVALID/USED states, member name, reward, date, and approve button). Created `functions/utils/qrHelper.js` and `functions/utils/emailTemplate.js`. Updated `RewardsStoreScreen.js` and `RewardsScreen.js` success messages to direct users to check email. Updated `MyRedemptionsScreen.js` to handle new `active`/`used`/`expired` statuses. Created `functions/REWARDS_EMAIL_SETUP.md` with step-by-step Resend configuration guide. Installed `resend` + `qrcode` packages in functions. **To activate:** set `RESEND_API_KEY` secret in Firebase, verify sender domain, update `WEB_APP_URL` constant, deploy functions + `public/` to Firebase Hosting.
* [2026-04-07] - Agent 1 (Claude): **Activated & deployed the Rewards Email System to production.** (1) Added `hosting` block to `firebase.json` pointing to `public/` so `verify.html` deploys to Firebase Hosting. (2) Updated `WEB_APP_URL` constant in `functions/index.js` to `https://ruvo-app-99c85.web.app` (real project domain). (3) Added `runWith({ secrets: ["RESEND_API_KEY"] })` to `redeemReward` Cloud Function so the key is securely injected at runtime. (4) Created Firebase Secret Manager secret `RESEND_API_KEY` with Resend API key `re_VKrNUAXr_*`. (5) Deployed hosting — `verify.html` now live at `https://ruvo-app-99c85.web.app/verify.html`. (6) Deployed all Cloud Functions successfully. System is fully operational: user redeems → coins deducted → QR code email sent via Resend → store staff scans QR → verification page → approve button → code marked used in Firestore.
* [2026-04-07] - Agent 1 (Claude): **Coin Economy Audit (Test A.1.1)** — Enhanced `saveRunActivity` Cloud Function with multi-factor coin calculation: (1) **Base rate**: 10 coins/km. (2) **Pace bonus**: <5min/km +20%, 5-6min +15%, 6-7min +10%, 7-8min +5%, >8min 0%. (3) **Streak bonus**: 7+ day streaks earn +5% bonus. (4) **Time-of-day dynamic pricing**: Off-peak (6-8am, 11am-1pm, 2-5pm) +10%, Peak (6-9pm) -10%. Added `coinBreakdown` object to response (base, paceBonus, streakBonus, timeBonus). Updated `addRunToHistory` in UserContext.js to return breakdown. Updated SaveActivityScreen.js to display breakdown below coin count (e.g., "+5 pace +3 streak +10 off-peak"). Created `docs/COIN_ECONOMY_TESTS.md` with test scenarios and validation matrix. All calculations are server-side (client passes only raw runEntry), uses Firestore transactions for atomic coin updates, and ensures non-negative values.

